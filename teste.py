import math

import numpy as np
import cmath
dataset = np.array([
    [0, 0],
    [0, 1],
    [1, 0],
    [1, 1]
])
rotulos = np.array([0, 1, 1, 1])

dataset_e_rotulos = zip(dataset, rotulos)
dataset = np.array([
    [0, 0],
    [0, 1],
    [1, 0],
    [1, 1]
])

arr = np.array([1, 2, 3, 4])

print(1 - arr)